"use client";

/**
 * Minimal background component — replaces the old pastel gradient blobs.
 * Now provides just a subtle fixed background for the page.
 * The main visual depth comes from the bg-noise utility on the body.
 */
const PastelGradientBackground = () => {
  return null;
};

export default PastelGradientBackground;
